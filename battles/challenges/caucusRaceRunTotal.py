def caucusRace(values):
    firstRunTotal = 0
    for v in values:
        if 
        firstRunTotal += v
    pr('firstRunTotal')

def test():
    testeql(caucusRace([-1, 4, -1, 3, -2, 2, 2, -3, 1, 3, -2]), [1,3,5,8])
