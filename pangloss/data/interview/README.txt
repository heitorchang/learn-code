Competitive programming resources

HackerRank: Cracking the Coding Interview
CodeFights: Interview Practice

Uploading static page to surge.sh

Using the original HTML results in too many HTTP requests (one for each .js file), making the page load very slow.<br><br>The folder <code>only-interview</code> contains a script, consolidate.py, that joins all .js data files into one.

Printable page

The page <code>interview_printable.html</code> generates a long page with all cards, sorted by topic. The related script is js/printable.js
